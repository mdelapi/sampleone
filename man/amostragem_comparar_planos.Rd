% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-comparar-planos.R
\name{amostragem_comparar_planos}
\alias{amostragem_comparar_planos}
\title{Compare the variance of different sampling plans for the same problem}
\usage{
amostragem_comparar_planos(..., referencia = NULL)
}
\arguments{
\item{...}{Two or more named \code{sampling_result} objects (e.g.
\code{aas = aas_estima_media(...)}, \code{estratificada = estr_estima_media(...)}),
each with a \code{variancia} element containing \code{var_media} or \code{var_p}.}

\item{referencia}{Name of the plan to use as the baseline (denominator
of the EPA ratio). Defaults to the first plan supplied.}
}
\value{
A \code{sampling_result} object with a comparison table of
variances and EPA (design effect) ratios relative to \code{referencia}.
}
\description{
Convenience function that lines up the estimated variance obtained
under different sampling designs (as returned by \code{sampling_result}
objects from this package) so they can be compared directly, computing
the sampling-plan effect (EPA / design effect) of each one relative to
a chosen baseline. This operationalises the comparisons illustrated
throughout the course notes (Section 3.7.7, Examples 4.4-4.5, and the
design-effect discussion in Section 6.1.5).
}
\examples{
x <- rnorm(30, 50, 5)
plano_aas <- aas_estima_media(x, N = 300)
# (em um caso real, plano_estrat viria de estr_estima_media() com os
#  mesmos dados organizados em estratos)
amostragem_comparar_planos(aas = plano_aas)
}
