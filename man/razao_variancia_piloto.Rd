% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cap07-tamanho-guarda-chuva.R
\name{razao_variancia_piloto}
\alias{razao_variancia_piloto}
\title{Pilot-sample variance estimate for the ratio estimator}
\usage{
razao_variancia_piloto(
  x = NULL,
  y = NULL,
  n = NULL,
  sum_x = NULL,
  sum_y = NULL,
  sum_x2 = NULL,
  sum_y2 = NULL,
  sum_xy = NULL,
  N = NULL,
  X = NULL,
  ...
)
}
\arguments{
\item{x, y}{Optional raw numeric vectors (sample data).}

\item{n, sum_x, sum_y, sum_x2, sum_y2, sum_xy}{Optional pre-aggregated
summary statistics, used when raw data are not available (e.g. a
problem statement that only gives totals).}

\item{N, X}{Ignored (kept for signature symmetry with \code{\link[=razao_estima_R]{razao_estima_R()}}
so a full problem statement can be passed without error) -- \code{s_r^2}
itself does not require them.}

\item{...}{Ignored (see package overview on tolerated extra arguments).}
}
\value{
A \code{sampling_result} object with \code{s_r^2}.
}
\description{
Computes \code{s_r^2}, the pilot-sample estimate of the population variance
of the ratio estimator (Eq. 7.42 of the course notes), used as an input
to \code{\link[=razao_tamanho_R]{razao_tamanho_R()}}, \code{\link[=razao_tamanho_total]{razao_tamanho_total()}}, and
\code{\link[=razao_tamanho_media]{razao_tamanho_media()}} when no prior estimate of the variance is
available.
}
\examples{
x <- c(1, 30, 44, 20, 0, 10, 15, 5, 2, 50, 35, 25)
y <- c(2, 35, 50, 27, 1, 15, 17, 7, 0, 53, 35, 30)
razao_variancia_piloto(x = x, y = y)
}
