% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cap06-conglomerados-2etapas.R
\name{cong2_estima_proporcao}
\alias{cong2_estima_proporcao}
\title{Estimate the population proportion under two-stage cluster sampling}
\usage{
cong2_estima_proporcao(Mi, mi, pi_hat, Nc, M = NULL, conf = 0.95, ...)
}
\arguments{
\item{Mi}{Numeric vector with the population size of each sampled
cluster.}

\item{mi}{Numeric vector with the number of sampled elements in each
cluster.}

\item{pi_hat}{Numeric vector with the sample proportion of each
cluster (Eq. 6.24).}

\item{Nc}{Total number of clusters in the population.}

\item{M}{Ignored (kept for signature symmetry with
\code{\link[=cong2_estima_media]{cong2_estima_media()}} and so that extra arguments from a broader
problem statement can be passed without error) -- see Details.}

\item{conf}{Confidence level, default \code{0.95}.}

\item{...}{Ignored (see package overview on tolerated extra arguments).}
}
\value{
A \code{sampling_result} object with the estimated proportion, its
variance, and confidence interval.
}
\description{
Analogous to \code{\link[=cong2_estima_media]{cong2_estima_media()}} but for a population proportion,
following Eqs. (6.24)-(6.29) of the course notes. Note that, unlike
\code{\link[=cong2_estima_media]{cong2_estima_media()}}, the proportion estimator is inherently a
ratio-type estimator (structurally analogous to
\code{\link[=cong2_estima_razao_media]{cong2_estima_razao_media()}}) and therefore always uses the \emph{sample}
average cluster size \code{m_barra = mean(Mi)} in its variance formula, even
when the population totals \code{Nc}/\code{M} are known -- this is exactly how
Example 6.6 of the course notes computes it (using \code{m_barra = 52.2}
rather than \code{M/Nc = 50}, even though both \code{M} and \code{Nc} are known in
that example).
}
\examples{
Mi <- c(50, 65, 45, 48, 52, 58, 42, 66, 40, 56)
mi <- c(10, 13, 9, 10, 10, 12, 8, 13, 8, 11)
pi_hat <- c(0.4, 0.3077, 0.3333, 0.3, 0.3, 0.25, 0.375, 0.3077, 0.375, 0.3636)
cong2_estima_proporcao(Mi = Mi, mi = mi, pi_hat = pi_hat, Nc = 90)
}
