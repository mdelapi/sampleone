% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cap04-estratificada-tamanho.R
\name{estr_tamanho_proporcao}
\alias{estr_tamanho_proporcao}
\title{Sample size for stratified sampling: proportion (proportional / Neyman / cost)}
\usage{
estr_tamanho_proporcao(
  Nk,
  pk,
  d,
  conf = 0.95,
  ck = NULL,
  alocacao = c("proporcional", "neyman", "custo", "nenhuma"),
  arredondar = c("cima", "proximo"),
  ...
)
}
\arguments{
\item{Nk}{Numeric vector with the population size of each stratum.}

\item{pk}{Numeric vector with the population proportion of each
stratum.}

\item{d}{Tolerable margin of error.}

\item{conf}{Confidence level, default \code{0.95}.}

\item{ck}{Optional numeric vector with the sampling cost per unit in
each stratum (Eq. 4.13).}

\item{alocacao}{How to allocate \code{n} across strata: \code{"proporcional"}
(default), \code{"neyman"}, \code{"custo"} (requires \code{ck}), or \code{"nenhuma"}.}

\item{arredondar}{Either \code{"cima"} (default) or \code{"proximo"} -- see
\code{\link[=estr_tamanho_media]{estr_tamanho_media()}}.}

\item{...}{Ignored (see package overview on tolerated extra arguments).}
}
\value{
A \code{sampling_result} object with the total sample size and,
unless \code{alocacao = "nenhuma"}, the per-stratum allocation.
}
\description{
Analogous to \code{\link[=estr_tamanho_media]{estr_tamanho_media()}} but for a population proportion,
using Eq. (4.12), with per-stratum proportions substituting for
variances (\code{sigmak2 = pk*(1-pk)}).
}
\examples{
estr_tamanho_proporcao(Nk = c(1540, 770, 385), pk = c(0.75, 0.50, 0.25), d = 0.05)
}
