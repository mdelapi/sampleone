% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cap07-razao.R
\name{.razao_resumo}
\alias{.razao_resumo}
\title{Compute summary statistics needed by the ratio estimator}
\usage{
.razao_resumo(
  x = NULL,
  y = NULL,
  n = NULL,
  sum_x = NULL,
  sum_y = NULL,
  sum_x2 = NULL,
  sum_y2 = NULL,
  sum_xy = NULL
)
}
\arguments{
\item{x, y}{Optional raw numeric vectors.}

\item{n, sum_x, sum_y, sum_x2, sum_y2, sum_xy}{Optional pre-aggregated
summary statistics (used only if \code{x}/\code{y} are not supplied).}
}
\value{
A list with \code{n}, \code{sum_x}, \code{sum_y}, \code{sum_x2}, \code{sum_y2},
\code{sum_xy}, \code{r}, and \code{soma_dyx2} (= \code{sum((y - r*x)^2)}, via the
computational Eq. 7.40).
}
\description{
Internal helper that accepts either raw vectors \code{x}/\code{y} or
pre-aggregated sums, and returns a common set of summary statistics
(\code{n}, \code{sum_x}, \code{sum_y}, \code{sum_x2}, \code{sum_y2}, \code{sum_xy}, \code{r}) used by the
\verb{razao_*()} functions. Supporting both calling styles matters because
some textbook problems (e.g. Example 7.5 of the course notes) give
only pre-computed sums, not the raw sample data.
}
\keyword{internal}
