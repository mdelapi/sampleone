% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cap07-razao.R
\name{razao_estima_media}
\alias{razao_estima_media}
\title{Ratio estimator of the population mean}
\usage{
razao_estima_media(
  x = NULL,
  y = NULL,
  n = NULL,
  sum_x = NULL,
  sum_y = NULL,
  sum_x2 = NULL,
  sum_y2 = NULL,
  sum_xy = NULL,
  N,
  X = NULL,
  conf = 0.95,
  ...
)
}
\arguments{
\item{x, y}{Optional raw numeric vectors (sample data).}

\item{n, sum_x, sum_y, sum_x2, sum_y2, sum_xy}{Optional pre-aggregated
summary statistics, used when raw data are not available (e.g. a
problem statement that only gives totals).}

\item{N}{Population size.}

\item{X}{Optional population total of the auxiliary variable; not
needed to compute the mean itself (only \code{N} is required), but
accepted here so that a full problem statement (which usually
supplies \code{X}) can be passed without error. Ignored if \code{NULL}.}

\item{conf}{Confidence level, default \code{0.95}.}

\item{...}{Ignored (see package overview on tolerated extra arguments).}
}
\value{
A \code{sampling_result} object with the estimated mean, its
variance, and confidence interval.
}
\description{
Estimates the population mean using the ratio estimator
(Eq. 7.36-7.38 of the course notes). Unlike \code{\link[=razao_estima_R]{razao_estima_R()}}, this
does not require knowing \code{X} (the population total of the auxiliary
variable) -- only \code{N}.
}
\examples{
x <- c(1, 30, 44, 20, 0, 10, 15, 5, 2, 50, 35, 25)
y <- c(2, 35, 50, 27, 1, 15, 17, 7, 0, 53, 35, 30)
razao_estima_media(x = x, y = y, N = 1500)
}
