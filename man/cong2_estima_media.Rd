% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cap06-conglomerados-2etapas.R
\name{cong2_estima_media}
\alias{cong2_estima_media}
\title{Estimate the population mean under two-stage cluster sampling}
\usage{
cong2_estima_media(
  dados = NULL,
  Mi = NULL,
  mi = NULL,
  yi_bar = NULL,
  si2 = NULL,
  Nc,
  M,
  conf = 0.95,
  ...
)
}
\arguments{
\item{dados}{A \code{data.frame} (or \code{list} of data frames / a list with
one element per cluster) with, for every sampled element, at least
the columns \code{conglomerado} (cluster id), \code{Mi} (cluster population
size), and \code{y} (the measured value for that element). Alternatively,
supply \code{Mi}, \code{mi}, \code{yi_bar}, and \code{si2} directly (pre-summarised per
cluster) -- see Details.}

\item{Mi}{Optional numeric vector with the population size of each
sampled cluster (used instead of \code{dados} when the data are already
summarised per cluster).}

\item{mi}{Optional numeric vector with the number of sampled elements
in each cluster.}

\item{yi_bar}{Optional numeric vector with the sample mean of each
cluster.}

\item{si2}{Optional numeric vector with the sample variance within
each cluster (Eq. 6.15).}

\item{Nc}{Total number of clusters in the population.}

\item{M}{Total number of elements in the population.}

\item{conf}{Confidence level, default \code{0.95}.}

\item{...}{Ignored (see package overview on tolerated extra arguments).}
}
\value{
A \code{sampling_result} object with the estimated mean, its
two-part variance decomposition, and confidence interval.
}
\description{
Estimates the population mean from a two-stage cluster sample (a sample
of clusters -- UPA -- followed by a sample of elements within each
sampled cluster -- USA), when the total population size \code{M} is known,
following Eqs. (6.12)-(6.18) of the course notes.
}
\details{
Two calling styles are supported: (1) pass raw per-element data via
\code{dados} (a data.frame with one row per sampled element), or (2) pass
already-summarised per-cluster vectors \code{Mi}, \code{mi}, \code{yi_bar}, \code{si2}
directly. The second style is convenient when a problem statement (as
in a textbook exercise) already gives per-cluster summaries instead of
raw data -- any of \code{dados}/\code{Mi}/\code{mi}/\code{yi_bar}/\code{si2} not needed for the
chosen style are simply ignored.
}
\examples{
Mi <- c(50, 65, 45, 48, 52, 58, 42, 66, 40, 56)
mi <- c(10, 13, 9, 10, 10, 12, 8, 13, 8, 11)
yi_bar <- c(5.4, 4.0, 5.6667, 4.8, 4.3, 3.8333, 5.0, 3.8462, 4.875, 5.0)
si2 <- c(11.3778, 10.6667, 16.75, 13.2889, 11.1222, 14.8788, 5.1429,
         4.3077, 6.125, 11.8)
cong2_estima_media(Mi = Mi, mi = mi, yi_bar = yi_bar, si2 = si2,
                    Nc = 90, M = 4500)
}
\references{
Cochran, W.G. (1977). \emph{Sampling Techniques}, 3rd ed. Wiley, cap. 10.
}
