% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cap03-aas.R
\name{aas_estima_proporcao}
\alias{aas_estima_proporcao}
\title{Estimate a population proportion under simple random sampling (AAS)}
\usage{
aas_estima_proporcao(
  x,
  N = NULL,
  reposicao = c("sem", "com"),
  conf = 0.95,
  ...
)
}
\arguments{
\item{x}{Numeric or logical vector coded 0/1 (or \code{FALSE}/\code{TRUE}).}

\item{N}{Optional population size. If \code{NULL} or \code{Inf}, the population is
treated as unknown/very large (the finite-population correction
\code{1 - n/N} is dropped, i.e. \code{f = 0}), which also makes the "com"
("with replacement") and "sem" ("without replacement") results
coincide.}

\item{reposicao}{Either \code{"sem"} (without replacement, AASs -- the
default and the usual case in practice) or \code{"com"} (with replacement,
AASc).}

\item{conf}{Confidence level, default \code{0.95}.}

\item{...}{Ignored (see package overview on tolerated extra arguments).}
}
\value{
A \code{sampling_result} object with the estimated proportion, its
variance, and confidence interval.
}
\description{
Estimates a population proportion from a simple random sample, with or
without replacement. Corresponds to Eqs. (3.16)-(3.21) (AASc) and
(3.39)-(3.44) (AASs) of the course notes.
}
\examples{
aas_estima_proporcao(c(1, 0, 1, 1, 0, 1, 0, 0), N = 500)
}
\references{
Cochran, W.G. (1977). \emph{Sampling Techniques}, 3rd ed. Wiley.
}
