% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cap03-aas.R
\name{aas_tamanho_media}
\alias{aas_tamanho_media}
\title{Sample size to estimate a population mean (AAS)}
\usage{
aas_tamanho_media(
  sigma2 = NULL,
  sigma = NULL,
  d,
  conf = 0.95,
  N = NULL,
  perda = 0,
  arredondar = c("cima", "proximo"),
  ...
)
}
\arguments{
\item{sigma2}{Population variance (or its estimate). Alternatively
supply \code{sigma} (standard deviation); if both are given, \code{sigma2}
takes precedence.}

\item{sigma}{Optional population standard deviation, used only if
\code{sigma2} is not supplied.}

\item{d}{Tolerable margin of error (same units as the variable).}

\item{conf}{Confidence level, default \code{0.95}.}

\item{N}{Optional population size. If \code{NULL} or \code{Inf}, the
infinite/unknown-population formula (3.15) is used; otherwise the
finite-population formula (3.37) is used (cross-checked internally
against the algebraically equivalent Eq. 3.38).}

\item{perda}{Optional flat safety-margin proportion (e.g. \code{0.25} for a
25\% buffer), applied via \code{aas_ajustar_perdas()} to inflate the raw
sample size (\code{n * (1 + perda)}, following Example 3.14 of the course
notes). Not to be confused with a formal response-rate adjustment
(dividing by an expected response rate, Section 4.7.2).}

\item{arredondar}{Either \code{"cima"} (the default -- always round up,
the conventional, conservative practice in sampling theory, e.g.
Cochran 1977) or \code{"proximo"} (round to the nearest integer). The
course notes round to the nearest integer using a 2-decimal critical
value (\code{z = 1.96}/\code{1.64}); use \code{arredondar = "proximo"} together
with a matching \code{conf} to reproduce the worked examples exactly.}

\item{...}{Ignored (see package overview on tolerated extra arguments).}
}
\value{
A \code{sampling_result} object with the required sample size.
}
\description{
Computes the sample size needed to estimate a population mean with a
given margin of error, choosing automatically between the
infinite/unknown-population formula (Eq. 3.15) and the finite-population
formula (Eq. 3.37/3.38) depending on whether \code{N} is supplied. Extra
arguments not needed for the chosen formula are ignored.
}
\examples{
aas_tamanho_media(sigma = 7, d = 2, conf = 0.95)
aas_tamanho_media(sigma2 = 1240, d = 7, conf = 0.95, N = 10000)
}
\references{
Cochran, W.G. (1977). \emph{Sampling Techniques}, 3rd ed. Wiley.
}
