% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-ajustes.R
\name{amostragem_ajustar_deff_resposta}
\alias{amostragem_ajustar_deff_resposta}
\title{Adjust a sample size for design effect and expected response rate}
\usage{
amostragem_ajustar_deff_resposta(n_prime, deff = 1.5, taxa_resposta = 0.85)
}
\arguments{
\item{n_prime}{Base sample size (e.g. from \code{\link[=aas_tamanho_media]{aas_tamanho_media()}},
\code{\link[=aas_tamanho_proporcao]{aas_tamanho_proporcao()}}, \code{\link[=estr_tamanho_media]{estr_tamanho_media()}}, or
\code{\link[=cong1_tamanho_media]{cong1_tamanho_media()}}), ignoring the design effect and non-response.}

\item{deff}{Design effect (\verb{>= 1}), default \code{1.5} as commonly assumed
in the absence of better information. \code{deff = 1} means no loss of
efficiency relative to simple random sampling.}

\item{taxa_resposta}{Expected response rate, default \code{0.85}.}
}
\value{
A \code{sampling_result} object with the adjusted sample size.
}
\description{
Inflates a base sample size (computed under a simple-random-sampling
assumption) by a design effect (\code{deff}, accounting for the extra
variability introduced by a more complex design such as clustering or
multi-stage sampling) and by an expected response rate, following
Eq. (6.6) of the course notes (Espinosa et al., 2019).
}
\details{
Although this formula is first introduced in the course notes' cluster
sampling chapter, it is a \strong{general-purpose adjustment}: it only needs
a base sample size, a design effect, and a response rate -- nothing
specific to clusters. Course exercises apply it to plain AAS and
stratified problems just as often as to cluster problems (e.g. to
scale up a prevalence-survey sample size for an anticipated design
effect and non-response), so it is exposed here as a cross-cutting
utility rather than tucked away under the \code{cong1_} prefix.
\code{\link[=cong1_ajustar_deff]{cong1_ajustar_deff()}} remains available as an alias, for continuity
with the cluster-sampling chapter and its documentation.
}
\examples{
# Cap. 3 (AAS) + deff + resposta -- ex.: planejamento de prevalencia
n_prime <- aas_tamanho_proporcao(p = 0.20, d = 0.05)$estimativa$n
amostragem_ajustar_deff_resposta(n_prime, deff = 1.2, taxa_resposta = 0.85)
}
\references{
Espinosa et al. (2019), as cited in the course notes, Eq. (6.6).
}
