% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cap05-sistematica.R
\name{sist_intervalo}
\alias{sist_intervalo}
\title{Sampling interval for systematic sampling}
\usage{
sist_intervalo(N, n, ...)
}
\arguments{
\item{N}{Population size.}

\item{n}{Desired sample size.}

\item{...}{Ignored (see package overview on tolerated extra arguments).}
}
\value{
A \code{sampling_result} object with the sampling interval (and,
when \code{N/n} is not an integer, the correction constant \verb{k*} needed by
\code{\link[=sist_selecionar_amostra]{sist_selecionar_amostra()}}).
}
\description{
Computes the sampling interval \code{k = N/n} used to select a systematic
sample, following Section 5.1-5.2 of the course notes. When \code{N/n} is
not an integer, \code{k} is first rounded up; if \code{k*n > N}, \code{k} is then
decremented by 1, and the correction constant \verb{k* = N - k*n} is
computed (Example 5.2 of the course notes).
}
\examples{
sist_intervalo(N = 2000, n = 80)
sist_intervalo(N = 2000, n = 75)
}
