% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cap03-aas-guarda-chuva.R
\name{aas_tamanho_amostra}
\alias{aas_tamanho_amostra}
\title{Sample size for AAS: automatic formula selection (umbrella function)}
\usage{
aas_tamanho_amostra(
  parametro = c("media", "proporcao"),
  N = NULL,
  sigma2 = NULL,
  sigma = NULL,
  p = NULL,
  d,
  conf = 0.95,
  perda = 0,
  arredondar = c("cima", "proximo"),
  ...
)
}
\arguments{
\item{parametro}{Either \code{"media"} or \code{"proporcao"}: which population
parameter the sample size should be computed for.}

\item{N}{Optional population size.}

\item{sigma2}{Population variance (used only when \code{parametro = "media"}).}

\item{sigma}{Population standard deviation (alternative to \code{sigma2},
used only when \code{parametro = "media"}).}

\item{p}{Expected proportion (used only when \code{parametro = "proporcao"}).}

\item{d}{Tolerable margin of error.}

\item{conf}{Confidence level, default \code{0.95}.}

\item{perda}{Optional expected non-response/loss rate in \verb{[0, 1)}.}

\item{arredondar}{Either \code{"cima"} (default) or \code{"proximo"} -- see
\code{\link[=aas_tamanho_media]{aas_tamanho_media()}}.}

\item{...}{Ignored. Any other variables from a broader problem statement
(e.g. sample data, stratum information) may be passed here without
causing an error.}
}
\value{
A \code{sampling_result} object, as produced by
\code{\link[=aas_tamanho_media]{aas_tamanho_media()}} or \code{\link[=aas_tamanho_proporcao]{aas_tamanho_proporcao()}}.
}
\description{
Convenience "umbrella" function that accepts every input that might be
relevant to determine an AAS sample size (population variance or
standard deviation, expected proportion, population size, expected
losses) and automatically dispatches to \code{\link[=aas_tamanho_media]{aas_tamanho_media()}} or
\code{\link[=aas_tamanho_proporcao]{aas_tamanho_proporcao()}} depending on \code{parametro}, silently ignoring
whichever inputs are not needed for the chosen calculation.
}
\examples{
aas_tamanho_amostra(parametro = "media", sigma = 7, d = 2)
aas_tamanho_amostra(parametro = "proporcao", p = 0.6, d = 0.03, N = 5000)
}
