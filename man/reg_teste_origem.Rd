% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cap07-regressao.R
\name{reg_teste_origem}
\alias{reg_teste_origem}
\title{Decide between the ratio and regression estimators}
\usage{
reg_teste_origem(x, y, conf = 0.95, ...)
}
\arguments{
\item{x}{Numeric vector with the auxiliary variable.}

\item{y}{Numeric vector with the variable of interest.}

\item{conf}{Confidence level, default \code{0.95}.}

\item{...}{Ignored (see package overview on tolerated extra arguments).}
}
\value{
A \code{sampling_result} object stating the recommended estimator,
the test statistic, the confidence interval for \code{a0}, and both
estimators computed side by side for comparison.
}
\description{
Tests whether the regression line of \code{y} on \code{x} passes through the
origin (\code{H0: a0 = 0}), using both the t-statistic (Eq. 7.20) and the
confidence interval for \code{a0} (Eq. 7.21), and recommends the ratio
estimator if the line passes through the origin (fails to reject
\code{H0}) or the regression estimator otherwise -- following the logic of
Examples 7.1-7.2 of the course notes.
}
\examples{
x <- c(1, 30, 44, 20, 0, 10, 15, 5, 2, 50, 35, 25)
y <- c(2, 35, 50, 27, 1, 15, 17, 7, 0, 53, 35, 30)
reg_teste_origem(x, y)
}
