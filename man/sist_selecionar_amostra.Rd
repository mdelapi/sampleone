% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cap05-sistematica.R
\name{sist_selecionar_amostra}
\alias{sist_selecionar_amostra}
\title{Select a systematic sample}
\usage{
sist_selecionar_amostra(N, n, r = NULL, semente = NULL, ...)
}
\arguments{
\item{N}{Population size.}

\item{n}{Desired sample size.}

\item{r}{Optional random start (an integer between 1 and \code{k},
inclusive). If \code{NULL}, one is drawn uniformly at random.}

\item{semente}{Optional integer seed for reproducibility. The user's
global random state is saved and restored, so calling this function
does not affect subsequent unrelated random draws.}

\item{...}{Ignored (see package overview on tolerated extra arguments).}
}
\value{
A \code{sampling_result} object with the selected element positions
(\code{1:N}).
}
\description{
Selects a systematic sample of size \code{n} from a population of size \code{N},
handling both the integer-interval case and the non-integer-interval
case (with the correction constant \verb{k*}), following the selection
rules of Section 5.2 of the course notes (Examples 5.1-5.2). In the
non-integer case, the first selected element is \code{r}; from the second
element onward, \verb{k*} is added on top of the regular step \code{k}
(i.e. element \code{i} for \code{i >= 2} equals \verb{(i-1)*k + k*} plus \code{r}), exactly as
illustrated in Example 5.2.
}
\examples{
sist_selecionar_amostra(N = 2000, n = 80, r = 8)
sist_selecionar_amostra(N = 2000, n = 75, r = 26)
}
