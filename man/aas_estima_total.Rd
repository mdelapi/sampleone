% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cap03-aas.R
\name{aas_estima_total}
\alias{aas_estima_total}
\title{Estimate the population total under simple random sampling (AAS)}
\usage{
aas_estima_total(x, N, reposicao = c("sem", "com"), conf = 0.95, ...)
}
\arguments{
\item{x}{Numeric vector with the sample data.}

\item{N}{Population size (required to scale the total).}

\item{reposicao}{Either \code{"sem"} (without replacement, AASs -- the
default and the usual case in practice) or \code{"com"} (with replacement,
AASc).}

\item{conf}{Confidence level, default \code{0.95}.}

\item{...}{Ignored (see package overview on tolerated extra arguments).}
}
\value{
A \code{sampling_result} object with the estimated total, its
variance, and confidence interval.
}
\description{
Estimates the population total \eqn{\hat{\tau} = N \bar{x}} from a
simple random sample without replacement (Eq. 3.28.2-3.28.3 of the
course notes). For the "with replacement" plan, the same expressions
are used with \code{f = 0}.
}
\examples{
aas_estima_total(c(12, 30, 18), N = 300)
}
\references{
Cochran, W.G. (1977). \emph{Sampling Techniques}, 3rd ed. Wiley, cap. 2 (extension not explicit in the course notes for AAS, but consistent with Eq. 3.28.3).
}
