% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base-sampling-result.R
\name{new_sampling_result}
\alias{new_sampling_result}
\title{Construct a sampling_result object}
\usage{
new_sampling_result(
  metodo,
  estimativa,
  variancia = NULL,
  intervalo_confianca = NULL,
  nivel_confianca = NULL,
  memoria_calculo = character(),
  interpretacao = NULL,
  insight = NULL,
  referencias = character(),
  formula_apostila = character(),
  dados_entrada = list()
)
}
\arguments{
\item{metodo}{Character. Short label for the sampling method/formula
used (e.g. \code{"AAS sem reposicao - media"}).}

\item{estimativa}{Named list with the main numeric result(s), e.g.
\code{list(media = 20, n = 47)}.}

\item{variancia}{Optional named list with variance/standard-error
components.}

\item{intervalo_confianca}{Optional numeric vector of length 2, or
\code{NULL} when not applicable.}

\item{nivel_confianca}{Numeric confidence level used, e.g. \code{0.95}.}

\item{memoria_calculo}{Character vector with a human-readable,
step-by-step trace of the calculation.}

\item{interpretacao}{Character scalar with a plain-language
interpretation of the result.}

\item{insight}{Character scalar with a short pedagogical suggestion
(e.g. pointing to a related method worth comparing).}

\item{referencias}{Character vector of bibliographic references backing
the formula used, beyond the course notes.}

\item{formula_apostila}{Character scalar identifying the equation
number(s) in the source course notes (e.g. \code{"(3.15)"}).}

\item{dados_entrada}{Named list with the (validated) inputs actually
used by the function, for transparency and reproducibility.}
}
\value{
An object of class \code{sampling_result}.
}
\description{
Internal constructor for the S3 class shared by every estimation and
sample-size function in the package. It bundles the point estimate,
variance/standard error, confidence interval, a step-by-step record of
the calculation, and a short pedagogical note into a single object so
that every function in \code{sampleone} returns a consistent, richly
documented result rather than a bare number.
}
\keyword{internal}
