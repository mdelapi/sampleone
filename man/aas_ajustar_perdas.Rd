% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cap03-aas.R
\name{aas_ajustar_perdas}
\alias{aas_ajustar_perdas}
\title{Adjust a sample size by a flat percentage buffer ("acrescimo")}
\usage{
aas_ajustar_perdas(n, taxa_acrescimo, arredondar = c("cima", "proximo"))
}
\arguments{
\item{n}{Base (unadjusted) sample size.}

\item{taxa_acrescimo}{Flat buffer to add, as a proportion (e.g. \code{0.25}
for a 25\% increase).}

\item{arredondar}{Either \code{"cima"} (default, always round up) or
\code{"proximo"} (round to the nearest integer, reproducing Example 3.14
of the course notes exactly: \code{357 * 1.25 = 446.25}, rounded there to
446, not 447).}
}
\value{
A \code{sampling_result} object with the adjusted sample size.
}
\description{
Inflates a sample size by a flat percentage buffer, following
Example 3.14 of the course notes: \code{n_ajustado = n * (1 + taxa_acrescimo)}
(there illustrated as a 25\% buffer added to protect against losses,
refusals, or other contingencies during fieldwork). This is a distinct,
simpler convention from the formal non-response adjustment of Section
4.7.2 (dividing by an expected response rate, see the course notes'
Eq. 4.25 and the package's stratified-sampling module) -- do not
confuse the two: dividing by \code{(1 - taxa)} targets a \emph{given number of
completed responses} after losses, while multiplying by \code{(1 + taxa)} is
a simpler flat safety margin, as used here.
}
\examples{
aas_ajustar_perdas(357, 0.25) # arredondar="cima" (padrao) -> 447
aas_ajustar_perdas(357, 0.25, arredondar = "proximo") # Exemplo 3.14 -> 446
}
