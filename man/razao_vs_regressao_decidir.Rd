% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cap07-tamanho-guarda-chuva.R
\name{razao_vs_regressao_decidir}
\alias{razao_vs_regressao_decidir}
\title{Ratio vs. regression: full side-by-side comparison (umbrella function)}
\usage{
razao_vs_regressao_decidir(x, y, X_barra, N, conf = 0.95, ...)
}
\arguments{
\item{x}{Numeric vector with the auxiliary variable.}

\item{y}{Numeric vector with the variable of interest.}

\item{X_barra}{Known population mean of the auxiliary variable
(required for the regression estimator; not required for the ratio
estimator, whose mean form only needs \code{N}).}

\item{N}{Population size.}

\item{conf}{Confidence level, default \code{0.95}.}

\item{...}{Ignored. Any other variables from a broader problem
statement may be passed here without causing an error.}
}
\value{
A named list with elements \code{teste} (from \code{\link[=reg_teste_origem]{reg_teste_origem()}}),
\code{razao} (from \code{\link[=razao_estima_media]{razao_estima_media()}}), and \code{regressao} (from
\code{\link[=reg_estima_media]{reg_estima_media()}}), plus a \code{recomendado} character scalar.
}
\description{
Convenience "umbrella" function that fits the regression model, tests
whether the line passes through the origin, and computes \emph{both} the
ratio and the regression estimators of the population mean side by
side, clearly flagging which one the origin test recommends --
following the full workflow of Examples 7.1-7.2 of the course notes.
This lets students and instructors compare both estimators even when
only one is formally "correct" for the data at hand.
}
\examples{
x <- c(1, 30, 44, 20, 0, 10, 15, 5, 2, 50, 35, 25)
y <- c(2, 35, 50, 27, 1, 15, 17, 7, 0, 53, 35, 30)
razao_vs_regressao_decidir(x, y, X_barra = 10, N = 1500)
}
