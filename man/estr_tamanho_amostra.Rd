% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cap04-estratificada-tamanho.R
\name{estr_tamanho_amostra}
\alias{estr_tamanho_amostra}
\title{Sample size for stratified sampling: automatic formula selection (umbrella)}
\usage{
estr_tamanho_amostra(
  parametro = c("media", "proporcao"),
  Nk,
  sigmak2 = NULL,
  pk = NULL,
  d,
  conf = 0.95,
  ck = NULL,
  alocacao = c("proporcional", "neyman", "custo", "nenhuma"),
  arredondar = c("cima", "proximo"),
  ...
)
}
\arguments{
\item{parametro}{Either \code{"media"} or \code{"proporcao"}.}

\item{Nk}{Numeric vector with the population size of each stratum.}

\item{sigmak2}{Numeric vector with the population variance of each
stratum.}

\item{pk}{Numeric vector with the population proportion of each stratum
(used only when \code{parametro = "proporcao"}).}

\item{d}{Tolerable margin of error.}

\item{conf}{Confidence level, default \code{0.95}.}

\item{ck}{Optional numeric vector with the sampling cost per unit in
each stratum. If supplied, the cost-based formula (Eq. 4.8) is used;
otherwise the standard formula (Eq. 4.5) is used.}

\item{alocacao}{How to allocate \code{n} across strata once computed:
\code{"proporcional"} (default), \code{"neyman"}, \code{"custo"} (requires \code{ck}), or
\code{"nenhuma"} (only the total \code{n} is returned).}

\item{arredondar}{Either \code{"cima"} (default, always round up -- the
conventional, conservative practice) or \code{"proximo"} (round to the
nearest integer, reproducing the course notes' worked examples,
which use a 2-decimal critical value and round to the nearest
integer).}

\item{...}{Ignored. Any other variables from a broader problem
statement may be passed here without causing an error.}
}
\value{
A \code{sampling_result} object, as produced by
\code{\link[=estr_tamanho_media]{estr_tamanho_media()}} or \code{\link[=estr_tamanho_proporcao]{estr_tamanho_proporcao()}}.
}
\description{
Convenience "umbrella" function analogous to \code{\link[=aas_tamanho_amostra]{aas_tamanho_amostra()}}:
accepts every input potentially relevant to a stratified sample-size
problem and dispatches to \code{\link[=estr_tamanho_media]{estr_tamanho_media()}} or
\code{\link[=estr_tamanho_proporcao]{estr_tamanho_proporcao()}} depending on \code{parametro}, ignoring whichever
inputs are not needed.
}
\examples{
estr_tamanho_amostra(parametro = "media", Nk = c(1540, 770, 385),
                      sigmak2 = c(7, 9, 11), d = 0.5)
}
